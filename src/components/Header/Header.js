/** @jsx jsx */
import { jsx } from "@emotion/core"
import PropTypes from "prop-types"

const style = {

}

const Header = (props) => {
  return (
    <div css={style}>
      Header
    </div>
  );
};

Header.propTypes = {

};

Header.defaultProps = {
  
};

export default Header;

/** @jsx jsx */
import { jsx } from "@emotion/core"
import PropTypes from "prop-types"

const style = {

}

const AdList = (props) => {
  return (
    <div css={style}>
      AdList
    </div>
  );
};

AdList.propTypes = {

};

AdList.defaultProps = {
  
};

export default AdList;

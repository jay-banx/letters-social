import React from "react";
import PropTypes from "prop-types"

import "./App.css"

const App = (props) => {
  return (
    <div className="app">
      App
    </div>
  );
};

App.propTypes = {

};

App.defaultProps = {
  
};

export default App;

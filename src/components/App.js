/** @jsx jsx */
import { jsx } from "@emotion/core"
import PropTypes from "prop-types"

const style = {

}

const App = (props) => {
  return (
    <div css={style}>
      App
    </div>
  );
};

App.propTypes = {

};

App.defaultProps = {
  
};

export default App;

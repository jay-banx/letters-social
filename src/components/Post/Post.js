/** @jsx jsx */
import { jsx } from "@emotion/core"
import PropTypes from "prop-types"

const style = {

}

const Post = (props) => {
  return (
    <div css={style}>
      Post
    </div>
  );
};

Post.propTypes = {

};

Post.defaultProps = {
  
};

export default Post;
